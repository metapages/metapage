// Deprecated alias for /convert/metapage/[targetversion] — see ../index.tsx.
// This one is a POST API, so it re-exports the handler rather than redirecting.
export { default, handler } from "../../convert/metapage/[targetversion].tsx";
