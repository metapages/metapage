// Deprecated alias for /convert/metaframe/[targetversion] — see ../index.tsx.
// This one is a POST API, so it re-exports the handler rather than redirecting.
export { default, handler } from "../../convert/metaframe/[targetversion].tsx";
