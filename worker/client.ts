// Client-side entry point. Import CSS or client-only modules here so Vite can
// bundle and hot-reload them. The site stylesheet (static/mp/style.css) is
// served as a static file and linked from routes/_app.tsx, and the /m and /mf
// renderers are standalone HTML documents, so there is nothing to import yet.
