# cloudseed cached deno source
**/.deno_dir

# cloudseed local
/.tmp

# Ignore the root .env file, child .env files are symlinked to this one
# so there is a SINGLE global .env context for a given operation
/.env

# I would love to commit this, but it's probably not a good idea
# /.vscode

# by default, do not commit the build documentation
/docs

###########################################################################################
# app/
###########################################################################################

# browser
**/node_modules
**/.cache
