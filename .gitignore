*.map
*.pyc
node_modules
*.sublime-workspace
tmp
npm-debug.log
.haxelib
.bundle
.idea
.aws
.env*
gitcache
_site
.sass-cache
libs/build
build/

# The generated lib files
docs/js/Meta*

# Normally .npmrc files are tracked, but we're only using ours
# to store the token for CI
.npmrc

# Markdeep, in case we are viewing this locally without internet
**/markdeep.min.js

# Local https certs
apps/https/conf.d/certs
